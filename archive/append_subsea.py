import json
import os

output_file = r'c:\Users\majip\Downloads\LLM REASEARCH\Local_Ollama_Evol_Pipeline\data\synthetic_generation_v3_enterprise.jsonl'

data = {
  'messages': [
    {
      'role': 'user',
      'content': 'You are acting as the Chief Subsea Telecom Engineer for a Global Submarine Cable Operator.\n\nYour mission is to generate a master-tier, production-ready IEC 61131-3 Structured Text (ST) implementation for the "Trans-Pacific Subsea Cable Repeater & Branching Unit (BU) Power Feed Controller" (`FB_Subsea_Telecom_PFE`).\n\n### Technical Specifications & Engineering Rigor Required:\n1. **$10,000\\text{ V}$ Constant Current Power Feed**:\n   - Closed-loop modulation of shore-based Power Feed Equipment (PFE) supplying a constant $1.0\\text{ Ampere}$ DC current through the copper conductor of a $10,000\\text{ km}$ subsea cable.\n   - Dynamic voltage regulation ($up\\ to\\ 10\\text{ kV}$) to overcome the massive resistive losses and power 100 Erbium-Doped Fiber Amplifier (EDFA) repeaters spaced every $100\\text{ km}$ along the ocean floor.\n2. **Subsea Branching Unit (BU) Fault Reconfiguration**:\n   - Instantaneous detection of cable cuts/shunts using Time Domain Reflectometry (TDR) and voltage drop anomalies.\n   - Autonomous execution of branching unit switching logic (using polarized latching relays) to re-route power from a secondary shore station without dropping the terabit/s optical data traffic on surviving branches.\n3. **Code Quality**: Strict IEC 61131-3 ST, LREAL datatypes, comprehensive `VAR_INPUT`/`VAR_OUTPUT`, Kirchhoff\'s laws equations for massively distributed networks, PackML states.'
    },
    {
      'role': 'assistant',
      'content': '```iec-st\nFUNCTION_BLOCK FB_Subsea_Telecom_PFE\nTITLE = \'Trans-Pacific Subsea Cable Repeater & Branching Unit (BU) Power Feed Controller\'\nVERSION : \'1.0\'\nAUTHOR : \'Lumina Elite Synthetic Data Architect\'\n\n(*\n    Subsea Telecom Power Feed Equipment (PFE) Controller\n    Features:\n    - 10kV Constant Current Power Feed (1.0 Ampere DC)\n    - Closed-loop voltage regulation based on Kirchhoff\'s Voltage Law (KVL)\n    - 10,000 km cable modeling, 100 EDFAs spacing\n    - Subsea Branching Unit (BU) Fault Reconfiguration\n    - TDR & Voltage Drop anomaly detection\n    - PackML State Machine implementation\n*)\n\nTYPE\n    E_PackML_State : (\n        eIDLE,\n        eSTARTING,\n        eEXECUTE,\n        eHOLDING,\n        eHELD,\n        eUNHOLDING,\n        eSUSPENDING,\n        eSUSPENDED,\n        eUNSUSPENDING,\n        eSTOPPING,\n        eSTOPPED,\n        eABORTING,\n        eABORTED,\n        eCLEARING\n    );\n    \n    E_Cable_Status : (\n        eHEALTHY,\n        eSHUNT_FAULT,\n        eOPEN_CIRCUIT_CUT,\n        eRECONFIGURING\n    );\nEND_TYPE\n\nVAR_INPUT\n    bEnable                 : BOOL;                 // PFE Enable Command\n    bStart                  : BOOL;                 // PackML Start Command\n    bStop                   : BOOL;                 // PackML Stop Command\n    bAbort                  : BOOL;                 // PackML Abort Command\n    bClear                  : BOOL;                 // PackML Clear Command\n    \n    // Analog Sensors\n    lrMeasuredCurrent       : LREAL;                // Output Current from PFE (A)\n    lrMeasuredVoltage       : LREAL;                // Output Voltage from PFE (V)\n    lrTDR_ReflectionTime    : LREAL;                // TDR pulse reflection time (ms)\n    \n    // Config\n    lrTargetCurrent         : LREAL := 1.0;         // Target Constant Current (A)\n    lrMaxVoltage            : LREAL := 10000.0;     // Maximum allowed Voltage (V)\n    lrCableResistancePerKm  : LREAL := 1.0;         // ohms per km\n    lrRepeaterVoltageDrop   : LREAL := 15.0;        // Voltage drop per EDFA repeater (V)\n    lrRepeaterSpacingKm     : LREAL := 100.0;       // Distance between repeaters (km)\n    lrTotalCableLengthKm    : LREAL := 10000.0;     // Total Cable length (km)\n    iTotalRepeaters         : INT := 100;           // Total Number of Repeaters\nEND_VAR\n\nVAR_OUTPUT\n    eState                  : E_PackML_State;       // Current PackML State\n    eCableStatus            : E_Cable_Status;       // Assessed Cable Status\n    \n    // PFE Control\n    lrCommandedVoltage      : LREAL;                // Voltage setpoint to PFE Hardware (V)\n    lrCommandedCurrent      : LREAL;                // Current setpoint to PFE Hardware (A)\n    \n    // Branching Unit Relays\n    bBU_SwitchPrimary       : BOOL;                 // Main Branching Unit Switch\n    bBU_SwitchSecondary     : BOOL;                 // Secondary / Backup Shore Station Switch\n    \n    // Alarms\n    bFaultDetected          : BOOL;\n    lrCalculatedFaultDist   : LREAL;                // Distance to fault (km)\nEND_VAR\n\nVAR\n    // PID Controller for Current Regulation\n    lrKp                    : LREAL := 500.0;       // Proportional Gain\n    lrKi                    : LREAL := 15.0;        // Integral Gain\n    lrError                 : LREAL;\n    lrIntegral              : LREAL;\n    \n    // Physical Calculations\n    lrExpectedVoltageDrop   : LREAL;\n    lrActualResistance      : LREAL;\n    lrSpeedOfLightCoax      : LREAL := 200000.0;    // km/s (approx signal speed in coax)\n    \n    // TDR processing\n    lrTDRDistance           : LREAL;\n    \n    // Diagnostics\n    lrVoltageAnomaly        : LREAL;\n    bAnomalyActive          : BOOL;\n    tAnomalyTimer           : TON;\nEND_VAR\n\nBEGIN\n    // PackML State Machine Transition Logic\n    CASE eState OF\n        eIDLE:\n            IF bStart THEN\n                eState := eSTARTING;\n            END_IF;\n            \n        eSTARTING:\n            // Initialize parameters\n            lrIntegral := 0.0;\n            lrCommandedVoltage := 0.0;\n            bBU_SwitchPrimary := TRUE;\n            bBU_SwitchSecondary := FALSE;\n            eCableStatus := eHEALTHY;\n            eState := eEXECUTE;\n            \n        eEXECUTE:\n            IF bStop THEN\n                eState := eSTOPPING;\n            ELSIF bAbort THEN\n                eState := eABORTING;\n            ELSIF bFaultDetected AND eCableStatus <> eRECONFIGURING THEN\n                eState := eHOLDING;\n            END_IF;\n            \n        eHOLDING:\n            // Reconfigure network\n            eCableStatus := eRECONFIGURING;\n            // Trip primary, switch to secondary shore station\n            bBU_SwitchPrimary := FALSE;\n            bBU_SwitchSecondary := TRUE;\n            eState := eHELD;\n            \n        eHELD:\n            IF bStart THEN // Unhold\n                eState := eUNHOLDING;\n            ELSIF bAbort THEN\n                eState := eABORTING;\n            END_IF;\n            \n        eUNHOLDING:\n            // Resume operations\n            eState := eEXECUTE;\n            \n        eSTOPPING:\n            lrCommandedVoltage := 0.0;\n            eState := eSTOPPED;\n            \n        eSTOPPED:\n            IF bClear THEN\n                eState := eCLEARING;\n            END_IF;\n            \n        eABORTING:\n            // Immediate cut of power\n            lrCommandedVoltage := 0.0;\n            bBU_SwitchPrimary := FALSE;\n            bBU_SwitchSecondary := FALSE;\n            eState := eABORTED;\n            \n        eABORTED:\n            IF bClear THEN\n                eState := eCLEARING;\n            END_IF;\n            \n        eCLEARING:\n            bFaultDetected := FALSE;\n            eCableStatus := eHEALTHY;\n            eState := eIDLE;\n            \n    END_CASE;\n\n    // Execute state logic for Active state\n    IF eState = eEXECUTE OR eState = eHELD THEN\n        \n        // -------------------------------------------------------------------\n        // 1. TDR (Time Domain Reflectometry) Distance Calculation\n        // Distance = (Speed * Time) / 2\n        // Time input is in ms, convert to seconds\n        // -------------------------------------------------------------------\n        IF lrTDR_ReflectionTime > 0.0 THEN\n            lrTDRDistance := (lrSpeedOfLightCoax * (lrTDR_ReflectionTime / 1000.0)) / 2.0;\n        ELSE\n            lrTDRDistance := lrTotalCableLengthKm;\n        END_IF;\n\n        // -------------------------------------------------------------------\n        // 2. Kirchhoff\'s Voltage Law (KVL) Modeling for Massive Distributed Network\n        // V_supply = sum(I * R_cable) + sum(V_drop_edfa) + V_shore_b\n        // Expected voltage at current draw\n        // -------------------------------------------------------------------\n        lrExpectedVoltageDrop := (lrTargetCurrent * lrTotalCableLengthKm * lrCableResistancePerKm) \n                                 + (INT_TO_LREAL(iTotalRepeaters) * lrRepeaterVoltageDrop);\n        \n        // Actual apparent resistance (Ohm\'s Law)\n        IF lrMeasuredCurrent > 0.001 THEN\n            lrActualResistance := lrMeasuredVoltage / lrMeasuredCurrent;\n        ELSE\n            lrActualResistance := 0.0;\n        END_IF;\n\n        // -------------------------------------------------------------------\n        // 3. Fault Detection: Voltage Drop Anomaly & TDR matching\n        // -------------------------------------------------------------------\n        lrVoltageAnomaly := ABS(lrMeasuredVoltage - lrExpectedVoltageDrop);\n        \n        // Shunt Fault (Short to sea ground) - Voltage drops significantly, current spikes\n        IF lrMeasuredVoltage < (lrExpectedVoltageDrop * 0.8) AND lrMeasuredCurrent >= lrTargetCurrent THEN\n            bAnomalyActive := TRUE;\n            eCableStatus := eSHUNT_FAULT;\n            lrCalculatedFaultDist := lrTDRDistance;\n        // Open Circuit (Cable Cut) - Current drops to near zero, voltage spikes\n        ELSIF lrMeasuredCurrent < (lrTargetCurrent * 0.1) THEN\n            bAnomalyActive := TRUE;\n            eCableStatus := eOPEN_CIRCUIT_CUT;\n            lrCalculatedFaultDist := lrTDRDistance;\n        ELSE\n            bAnomalyActive := FALSE;\n        END_IF;\n\n        // Debounce Faults\n        tAnomalyTimer(IN := bAnomalyActive, PT := T#50MS);\n        IF tAnomalyTimer.Q THEN\n            bFaultDetected := TRUE;\n        END_IF;\n\n        // -------------------------------------------------------------------\n        // 4. Constant Current Closed-Loop Control (PI Controller)\n        // -------------------------------------------------------------------\n        IF NOT bFaultDetected THEN\n            // Regulate to 1.0 A\n            lrError := lrTargetCurrent - lrMeasuredCurrent;\n            \n            // Integral accumulator (Anti-windup handled by clamping)\n            lrIntegral := lrIntegral + (lrError * lrKi * 0.01); // Assuming 10ms cycle time\n            \n            // PI Output\n            lrCommandedVoltage := (lrKp * lrError) + lrIntegral;\n            \n            // Clamp Voltage to Max Safe Limits (e.g., 10kV)\n            IF lrCommandedVoltage > lrMaxVoltage THEN\n                lrCommandedVoltage := lrMaxVoltage;\n                // Anti-windup\n                lrIntegral := lrMaxVoltage - (lrKp * lrError);\n            ELSIF lrCommandedVoltage < 0.0 THEN\n                lrCommandedVoltage := 0.0;\n                lrIntegral := 0.0;\n            END_IF;\n            \n            lrCommandedCurrent := lrTargetCurrent;\n        ELSE\n            // During a fault before hold state cuts it, try to maintain a safe lower probing voltage\n            lrCommandedVoltage := 500.0; // Probing voltage\n            lrCommandedCurrent := 0.1;\n        END_IF;\n\n    END_IF;\n\n    // Global Abort Override\n    IF bAbort THEN\n        eState := eABORTING;\n    END_IF;\n\nEND_FUNCTION_BLOCK\n```'
    }
  ]
}

with open(output_file, 'a', encoding='utf-8') as f:
    f.write(json.dumps(data) + '\n')

print('Success')
